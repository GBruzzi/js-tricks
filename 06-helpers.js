const schollName = 'Kennedy';

// includes ( verificar se existe tal caractere detro da string )
const includes = schollName.includes('enn');
console.log({ includes })

// startsWith ( se tal string começa com determinada sequencia de caractere )
const starts = schollName.startsWith('Ke')
console.log({ starts })

// endWith ( contrário do startsWith )
const end = schollName.endsWith('nnedy');
console.log({ end })